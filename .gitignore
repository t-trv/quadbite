# ----------------------------------------------------------------------
# 1. PHỤ THUỘC & THƯ MỤC BUILD (Dependencies & Build Artifacts)
# ----------------------------------------------------------------------

# Thư mục chứa các gói Node.js (luôn bỏ qua)
/node_modules

# Thư mục được tạo ra khi build/compile dự án
/dist
/build
/out
/coverage
/esnext

# Các tệp tạm thời của TypeScript (nếu không dùng thư mục build)
*.tsbuildinfo


# ----------------------------------------------------------------------
# 2. CẤU HÌNH CỦA PRISMA (Prisma Configurations)
# ----------------------------------------------------------------------

# Thư mục chứa Prisma Client đã được tạo ra (Generated Client)
# Thay thế đường dẫn này bằng thư mục 'output' bạn đã cấu hình nếu khác
/src/generated/prisma

# Nếu bạn không muốn commit thư mục Migrations (Không khuyến nghị, nhưng có thể cần)
# /prisma/migrations/


# ----------------------------------------------------------------------
# 3. MÔI TRƯỜNG & KHÓA BẢO MẬT (Environment & Security Keys)
# ----------------------------------------------------------------------

# Các tệp chứa biến môi trường (Database credentials, API keys, v.v.)
.env
.env.*
!.env.example # Giữ lại tệp mẫu (nếu có)
.pnpm-store # Pnpm cache

# Các khóa và chứng chỉ bảo mật
*.key
*.pem
*.p12


# ----------------------------------------------------------------------
# 4. LOGS & DATABASE
# ----------------------------------------------------------------------

# Tệp log
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Database (SQLite, thường dùng trong dev)
*.sqlite
*.sqlite-journal
/data*.db

# ----------------------------------------------------------------------
# 5. HỆ ĐIỀU HÀNH & IDE CỤ THỂ (OS & Specific IDE Files)
# ----------------------------------------------------------------------

# MacOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db

# JetBrains (IntelliJ, WebStorm, etc.)
.idea/

# VS Code
.vscode/
# Bỏ qua tệp cài đặt không gian làm việc cục bộ
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json